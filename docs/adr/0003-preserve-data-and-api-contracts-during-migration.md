# 迁移期间保持数据和 Leanote API 契约

> 客户端覆盖升级、发布签名连续性和 DBFlow 旧数据库迁移部分已由 ADR 0007 取代；Leanote 服务 API、USN、冲突、内容格式和附件契约部分继续有效。

现代化版本沿用原应用 ID 和签名进行覆盖升级，并保持现有 Leanote REST 路径、字段、错误响应、USN 分页、冲突语义、Markdown/HTML 内容和附件身份。DBFlow 数据库通过经真实旧版数据库快照验证的迁移原地交给 Room 和 Room FTS；迁移失败时保留原数据库并停止可能破坏数据的新版流程，绝不清库或用重新同步掩盖失败。
