# 首个公开版本不承担旧客户端升级兼容

当前没有部署给用户的旧版 APK，因此首个公开版本从 Android 14 和全新 Room 数据库开始，不实现旧 APK 覆盖升级、历史签名连续性或 DBFlow 数据库迁移；继续使用 `com.leanote.android` 作为应用身份，但这不是旧版本兼容承诺。该决定放弃未部署内部版本的数据升级路径，以避免为不存在的用户状态增加迁移风险，同时继续保持 Leanote 服务 REST、USN、冲突、Markdown/HTML 内容和附件契约。
